---
title: Components
description: All OUDS components at a glance.
aliases:
  - "/components/"
  - "/docs/components/"
  - "/docs/[[config:docs_version]]/components/"
toc: true
---

import {componentsDetails} from '../../../data/components-details'

## Alphabetical order

<ul class="list-unstyled row gy-large row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-xl-4 row-cols-2xl-5">
  {
    componentsDetails.map((component) => <ComponentCard component={component} idPrefix={'_alpha'} />)
  }
</ul>

<hr />

## By category

{
  ['Actions', 'Content display', 'Control', 'Data', 'Dialog', 'Indicator', 'Layout', 'Navigation', 'Visual assets'].map((element) => {
    return (
      <>
        {componentsDetails.filter((component) => component.category === element).length > 0 && <h3>{element}</h3>}
        <ul class="list-unstyled row gy-large row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-xl-4 row-cols-2xl-5">
          {componentsDetails.filter((component) => component.category === element).map((component) => <ComponentCard component={component} idPrefix={'_category'} />)}
        </ul>
      </>
    )
  })
}
