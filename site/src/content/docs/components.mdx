---
title: Components
description: All OUDS components at a glance.
aliases:
  - "/components/"
  - "/docs/components/"
  - "/docs/[[config:docs_version]]/components/"
toc: true
---

import { componentsDetails } from '../../../data/components-details'
import { getVersionedDocsPath } from '@libs/path'
import { ComponentCard } from '@components/shortcodes'

<Callout
  type="info"
  title="Component versioning"
  titleLevel={2}
  action="Check OUDS components status and version"
  actionHref={getVersionedDocsPath('/getting-started/component-versioning')}
>
  OUDS components are versioned to track updates to their design specification, ensuring consistency and traceability for users.
</Callout>

## Alphabetical order

<ul class="list-unstyled row gy-large row-cols-1 sm:row-cols-2 xl:row-cols-3 2xl:row-cols-4">
  {
    componentsDetails.map((component) => <ComponentCard component={component} idPrefix={'_alpha'} />)
  }
</ul>

<hr />

## By category

export const getComponentListForCategory = (category) => {
  return (
    <ul class="list-unstyled row gy-large row-cols-1 sm:row-cols-2 xl:row-cols-3 2xl:row-cols-4">
      {componentsDetails.filter((component) => component.category === category).map((component) => <ComponentCard component={component} idPrefix={'_category'} />)}
    </ul>
  )
}

{/* No loop here to have the headings in the toc */}

### Actions

{getComponentListForCategory('Actions')}

### Content display

{getComponentListForCategory('Content display')}

### Control

{getComponentListForCategory('Control')}

{/* ### Data

{getComponentListForCategory('Data')} */}

### Dialog

{getComponentListForCategory('Dialog')}

### Indicator

{getComponentListForCategory('Indicator')}

### Layout

{getComponentListForCategory('Layout')}

### Navigation

{getComponentListForCategory('Navigation')}

### Visual assets

{getComponentListForCategory('Visual assets')}
