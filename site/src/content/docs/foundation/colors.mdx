---
title: Colors
description: OUDS Web’s colors reinforces our brand identity and ensures consistency across all websites.
aliases:
  - "/docs/customize/color-palette/"
  - "/docs/foundation/color-palette/"
  - "/docs/foundation/colors/"
toc: true
---

import { getData } from '@libs/data'
import { getConfig } from '@libs/config'
import { allTokens, getTokenValue } from '@libs/sass-variables'
import { getVersionedDocsPath } from '@libs/path'
import { extractRootName, mergeTokensByMode } from '@components/shortcodes/ColorTable.astro'

export const getSurfaceColorValueFor = (colorName) => {
  let bgColor = colorName.replace('content-on', '$ouds-color')
  if(colorName.includes('-status-')) {
    bgColor = colorName.replace('content-on', '$ouds-color-surface')
  }
  return {bgLight: getTokenValue(`${bgColor}-light`), bgDark: getTokenValue(`${bgColor}-dark`)};
}

This page lists all the colors available in OUDS Web and their usage, organized by their category. All colors are presented in their light and dark mode for reference, but they must be used through their custom property or implementing utilities that will dynamically conform to the [color mode]([[docsref:/foundation/color-modes]]).

## Background

These colors are used for whole pages and structurally meaningful sections backgrounds.

These colors can be used with [our background utilities]([[docsref:/utilities/background]]).

<Callout type="info" title="Usage reference">
  Please refer to the [background colors design reference](https://unified-design-system.orange.com/472794e18/p/217ac6-color/b/11c27d) for more information on usage.
</Callout>

<ColorTable tokenFilter={(token) => /\$ouds-color-bg-.*-(light|dark)/.test(token.name)} />

## Surface

These colors should be used as background for elements that are on another background.

These colors can be used with [our background utilities]([[docsref:/utilities/background]]). Some of these surface colors have an opacity value and are designed to come on top of another color, we explain [how to achieve that here]([[docsref:/utilities/background#multiple-backgrounds]]).

<Callout type="info" title="Usage reference">
  Please refer to the [surface colors design reference](https://unified-design-system.orange.com/472794e18/p/217ac6-color/b/11c27d) for more information on usage.
</Callout>

<ColorTable tokenFilter={(token) => /\$ouds-color-surface-((?!status)).*-(light|dark)/.test(token.name)} />

### Status

These surface colors are used specifically to indicate a status.

Like other surface colors, they can be used with [our background utilities]([[docsref:/utilities/background]]).

<ColorTable tokenFilter={(token) => /\$ouds-color-surface-status.*-(light|dark)/.test(token.name)} />

## Content

These colors are used for informative but non-interactive content such as texts and icons.

These colors can be used with [our text color utilities]([[docsref:/utilities/colors]]).

<Callout type="info" title="Usage reference">
  Please refer to the [content colors design reference](https://unified-design-system.orange.com/472794e18/p/217ac6-color/b/11c27d) for more information on usage.
</Callout>

<ColorTable tokenFilter={(token) => /\$ouds-color-content((?!-on-)).*-(light|dark)/.test(token.name)} />

### Content on other colors

These content colors are used to guarantee maximum legibility for contents displayed on a background or surface color.

<Callout type="info">
  In this table we display an icon with the content color on their target background color.
</Callout>

{/* this table is quite specific so we do not use the component */}
<table class="table">
  <thead>
    <tr>
      <th>Custom property</th>
      <th>Light mode</th>
      <th>Dark mode</th>
    </tr>
  </thead>
  <tbody>
    {
      allTokens
        .filter((token) => /\$ouds-color-content-on-.*-(light|dark)/.test(token.name))
        .map(extractRootName)
        .reduce(mergeTokensByMode, [])
        .map((colorToken) => ({
          ...colorToken,
          ...getSurfaceColorValueFor(colorToken.rootName)
        }))
        .map((token) => (
          <tr class="align-middle">
            <td style="width: 50%;">
              <var class="user-select-all">{`var(--bs-color-${token.rootName})`}</var>
              <button
                type="button"
                class="btn btn-minimal btn-icon btn-clipboard color-copy"
                data-clipboard-text={`var(--bs-color-${token.rootName})`}
                title="Copy to clipboard"
                data-bs-toggle="tooltip"
              >
                <svg aria-hidden="true">
                  <use xlink:href={getVersionedDocsPath('/assets/img/ouds-web-sprite.svg#copy')} />
                </svg>
              </button>
            </td>
            <td>
              <figure class="ratio-21x9 mb-none d-flex justify-content-center align-items-center" style={{ backgroundColor: token.bgLight }}>
                <svg class="decorative-small-icon" x="15" y="15" width="20" height="20" aria-hidden="true" color={token.lightValue}>
                  <use xlink:href={getVersionedDocsPath('/assets/img/ouds-web-sprite.svg#accessibility-vision')}/>
                </svg>
                <figcaption class="visually-hidden">
                  <var>{token.lightValue}</var>
                </figcaption>
              </figure>
            </td>
            <td>
              <figure class="ratio-21x9 mb-none d-flex justify-content-center align-items-center" style={{ backgroundColor: token.bgDark }}>
                <svg class="decorative-small-icon" x="15" y="15" width="20" height="20" aria-hidden="true" color={token.darkValue}>
                  <use xlink:href={getVersionedDocsPath('/assets/img/ouds-web-sprite.svg#accessibility-vision')}/>
                </svg>
                <figcaption class="visually-hidden">
                  <var>{token.darkValue}</var>
                </figcaption>
              </figure>
            </td>
          </tr>
        ))
    }
  </tbody>
</table>

## Border

These colors are used for borders and dividers.

These colors can be used with [our border utilities]([[docsref:/utilities/border#color]]).

<Callout type="info" title="Usage reference">
  Please refer to the [border colors design reference](https://unified-design-system.orange.com/472794e18/p/217ac6-color/b/11c27d) for more information on usage.
</Callout>

<ColorTable tokenFilter={(token) => /\$ouds-color-border.*-(light|dark)/.test(token.name)} />

## Action

These colors are used for interactive elements and actions' states.

<Callout type="info" title="Usage reference">
  Please refer to the [action colors design reference](https://unified-design-system.orange.com/472794e18/p/217ac6-color/b/11c27d) for more information on usage.
</Callout>

<Callout type="warning">
  These colors should only be used for custom components development, this is why no utilities are provided, please use directly the custom properties in your CSS.
</Callout>

<ColorTable tokenFilter={(token) => /\$ouds-color-action.*-(light|dark)/.test(token.name)} />

## Overlay

These colors are used for overlay elements like dropdown, modals and tooltips to create depth and emphasis.

<Callout type="info" title="Usage reference">
  Please refer to the [overlay colors design reference](https://unified-design-system.orange.com/472794e18/p/217ac6-color/b/11c27d) for more information on usage.
</Callout>

<Callout type="warning">
  These colors should only be used for custom components development, this is why no utilities are provided, please use directly the custom properties in your CSS.
</Callout>

<ColorTable tokenFilter={(token) => /\$ouds-color-overlay.*-(light|dark)/.test(token.name)} />

## Always

These colors are the sole invariant colors defined by OUDS Web, they will stay the same on every brand, modes or contexts.

<Callout type="info" title="Usage reference">
  Please refer to the [always colors design reference](https://unified-design-system.orange.com/472794e18/p/217ac6-color/b/11c27d) for more information on usage.
</Callout>

<ColorTable tokenFilter={(token) => /\$ouds-color-always.*/.test(token.name)} />

## Decorative

<p>No tokens generated</p>
