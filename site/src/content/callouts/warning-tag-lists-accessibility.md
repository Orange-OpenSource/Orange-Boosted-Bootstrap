Tag and input tag lists must be wrapped in a semantic container (usually a `<ul>` or `<ol>`). *If possible, text that semantically describes the purpose of the tags*, otherwise add an `aria-label` to the container that explains the tags' function. [Read more about tags accessibility here](#accessibility).
