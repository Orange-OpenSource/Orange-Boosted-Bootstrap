---
import { getVersionedBsCssProps } from '@libs/oudsWeb'
import type { Layout } from '@libs/layout'

interface Props {
  direction?: 'rtl'
  layout: Layout
  bootstrap_compat_css?: boolean
}

const { direction,
  bootstrap_compat_css = true } = Astro.props
---

<link {...getVersionedBsCssProps(direction, bootstrap_compat_css)} />
