---
import BaseLayout from '@layouts/BaseLayout.astro'
import { getConfig } from '@libs/config'

interface Props {
  description: string
  title: string
}

const { description, title } = Astro.props
---

<BaseLayout {...Astro.props} layout="single">
  <Fragment slot="main">
    <main class="bd-content order-1" id="bd-content">
      <div class="title-bar mb-small md:mb-none">
        <div class="container-fluid container-max-width">
          <h1 class="display-large">{title}</h1>
        </div>
      </div>
      <div class="pb-2xlarge md:pb-small">
        <div class="container-fluid container-max-width bd-gutter md:pt-small md:pb-2xlarge">
          <div class="row">
            <div class="col-12">
              <p class="h3 mb-2xlarge">{description}</p>
              <slot />
            </div>
          </div>
        </div>
      </div>
      <div class="container-fluid container-max-width bd-gutter">
        <slot name="main-content" />
      </div>
    </main>
  </Fragment>
</BaseLayout>
