# Color and Background

> **Not Bootstrap:** The combined `.text-bg-*` helpers from Bootstrap should **not** be used in OUDS Web. Use color modes (`data-bs-theme`) with background utilities instead. See the background utility reference for proper usage.

Use [background utilities](background.md) combined with `data-bs-theme` attributes for accessible color/background pairings.
