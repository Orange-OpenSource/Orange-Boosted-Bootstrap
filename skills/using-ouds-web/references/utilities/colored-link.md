# Colored Links

> **Not Bootstrap:** The `.link-*` colored link classes from Bootstrap (`.link-primary`, `.link-secondary`, etc.) should **not** be used in OUDS Web. Use standard link styling instead.

Colored links are not part of the OUDS Web design system. Standard link styles are applied automatically.
