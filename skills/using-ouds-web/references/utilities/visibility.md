# Visibility

Control element visibility without modifying `display`. Hidden elements still occupy space in the layout.

## Classes

```html
<div class="visible">...</div>
<div class="invisible">...</div>
```

`.invisible` hides the element both visually and from assistive technologies. Use `.visually-hidden` instead to hide only visually while keeping screen reader access.
